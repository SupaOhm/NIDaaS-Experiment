# Core data science
pandas>=1.5.0           # DataFrame loading and manipulation
numpy>=1.23.0            # Numerical operations

# Machine learning — classical models
scikit-learn>=1.2.0      # RandomForest, LogisticRegression, scalers, metrics
xgboost>=1.7.0           # XGBoost classifier

# Deep learning — LSTM model
# TensorFlow does not yet support Python 3.14. We use PyTorch instead.
# Install command: pip install torch  (CPU-only, auto-detects Apple Silicon)
torch>=2.0.0             # PyTorch LSTM layers, batch normalisation

# Deduplication — Bloom filter
pybloom-live>=4.0.0      # Bloom filter implementation

# Visualisation
matplotlib>=3.6.0        # Figures and plots
seaborn>=0.12.0          # Heatmaps (confusion matrices)

# Progress bars
tqdm>=4.64.0             # Loop progress indicators

# Memory profiling for efficiency experiment
psutil>=5.9.0            # Process memory and CPU measurement
